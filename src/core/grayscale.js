export function toGrayscale(imageData){const {data,width,height}=imageData;const gray=new Uint8ClampedArray(width*height);const alpha=new Uint8ClampedArray(width*height);for(let i=0,p=0;i<data.length;i+=4,p++){gray[p]=Math.round(.299*data[i]+.587*data[i+1]+.114*data[i+2]);alpha[p]=data[i+3]}return {width,height,gray,alpha}}
export function grayToImageData(gray,w,h,levels=null){const out=new ImageData(w,h);for(let i=0;i<gray.length;i++){const v=levels?Math.round(255-(gray[i]/Math.max(1,levels-1))*255):gray[i];out.data[i*4]=out.data[i*4+1]=out.data[i*4+2]=v;out.data[i*4+3]=255}return out}
