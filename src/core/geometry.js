export const boundsOfMask=(mask,w,h)=>{let minX=w,minY=h,maxX=0,maxY=0,any=false;for(let y=0;y<h;y++)for(let x=0;x<w;x++)if(mask[y*w+x]){any=true;minX=Math.min(minX,x);maxX=Math.max(maxX,x);minY=Math.min(minY,y);maxY=Math.max(maxY,y)}return any?{minX,minY,maxX,maxY,width:maxX-minX+1,height:maxY-minY+1}:{minX:0,minY:0,maxX:w,maxY:h,width:w,height:h}}
export function simplifyPolyline(points,eps=2){if(points.length<3)return points;const d=(p,a,b)=>{const A=p.x-a.x,B=p.y-a.y,C=b.x-a.x,D=b.y-a.y;const dot=A*C+B*D,len=C*C+D*D,t=len?Math.max(0,Math.min(1,dot/len)):0;const x=a.x+t*C,y=a.y+t*D;return Math.hypot(p.x-x,p.y-y)};let max=0,idx=0;for(let i=1;i<points.length-1;i++){const dd=d(points[i],points[0],points.at(-1));if(dd>max){max=dd;idx=i}}if(max>eps){return [...simplifyPolyline(points.slice(0,idx+1),eps).slice(0,-1),...simplifyPolyline(points.slice(idx),eps)]}return [points[0],points.at(-1)]}
export const segmentLength=s=>Math.hypot(s.x2-s.x1,s.y2-s.y1);
